[package]
name = "starling-api"
description = "The feature contract: what a feature may see, do and return. Traits and values only."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# Only domain crates. A feature that depends on this cannot reach a service, a
# transport or the bus — which is the whole point of the crate existing.
[dependencies]
bytes.workspace = true
thiserror.workspace = true
inventory.workspace = true
starling-config.workspace = true
starling-crypto.workspace = true
starling-log.workspace = true
starling-gate.workspace = true
starling-model.workspace = true
starling-proto.workspace = true

[lints]
workspace = true
