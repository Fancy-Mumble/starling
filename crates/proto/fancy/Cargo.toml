[package]
name = "starling-proto-fancy"
description = "Fancy envelopes (one outer type per service) and the inter-service gRPC contracts"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
starling-proto.workspace = true
bitflags.workspace = true
prost.workspace = true
tonic.workspace = true
tonic-prost.workspace = true
# For the SHA-1 of a channel description, which murmur sends in place of the
# body once it reaches 128 bytes (`Mumble.proto`, ChannelState field 10).
sha1.workspace = true

[build-dependencies]
tonic-prost-build.workspace = true

[lints]
workspace = true
