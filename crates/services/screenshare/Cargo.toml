[package]
name = "starling-screenshare"
description = "screen-share signalling; media goes client to SFU directly"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
starling-proto-fancy.workspace = true
starling-runtime.workspace = true
# The media plane. Signalling is this service's half of screen sharing; the
# other half is a crate, not a process, so it is called rather than dialled.
starling-sfu.workspace = true
tokio.workspace = true

tracing.workspace = true
prost.workspace = true
tonic.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }

[lints]
workspace = true
