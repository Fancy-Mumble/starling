[package]
name = "starling-tls"
description = "The server's TLS identity: load a PEM pair, or generate and persist a self-signed one"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# A service, not domain: it reads and writes files. That is why it is not part of
# `starling-crypto`, which holds the pure negotiation policy and cipher specs.
[dependencies]
rcgen.workspace = true
rustls.workspace = true
rustls-pemfile.workspace = true
thiserror.workspace = true
tracing.workspace = true
