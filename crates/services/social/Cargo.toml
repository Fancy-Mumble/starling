[package]
name = "starling-social"
description = "reactions, receipts, typing, polls, watch-together and drawing"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# `PermissionDenied`, the one refusal the canon has for a vote.
starling-proto.workspace = true
starling-proto-fancy.workspace = true
starling-runtime.workspace = true
sqlx.workspace = true
# The retention sweep, and the lock that serialises a poll write with its read.
tokio.workspace = true

prost.workspace = true
tonic.workspace = true
tracing.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }

[lints]
workspace = true
