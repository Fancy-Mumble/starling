[package]
name = "starling-audit"
description = "the hash-chained operator record"
version = "0.2.2"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
starling-proto-fancy.workspace = true
starling-runtime.workspace = true
# The profile history: an avatar is kept shrunk and a comment compressed.
starling-imaging.workspace = true
zstd.workspace = true
sha2.workspace = true
sqlx.workspace = true

prost.workspace = true
# The retention sweeper is a timer task, so this is a runtime dependency now
# rather than only a test one.
tokio.workspace = true
tonic.workspace = true
tracing.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }
# An encoder, to hand the avatar path a real picture.
image.workspace = true

[lints]
workspace = true
