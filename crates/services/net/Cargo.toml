[package]
name = "starling-net"
description = "Transport: the TLS listener, per-connection tasks, and frame delivery"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# Implements `starling-api`'s `Outbound` and `FrameSink`; the state service
# consumes those traits and never names this crate.
[dependencies]
bytes.workspace = true
socket2.workspace = true
starling-api.workspace = true
starling-crypto.workspace = true
starling-proto.workspace = true
starling-server.workspace = true
starling-tls.workspace = true
thiserror.workspace = true
tokio.workspace = true
tokio-rustls.workspace = true
tracing.workspace = true

[lints]
workspace = true
