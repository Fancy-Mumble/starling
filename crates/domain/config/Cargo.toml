[package]
name = "starling-config"
description = "The server's resolved settings — plain values, no I/O and no file format"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# Nothing. Configuration is values; who reads a file and who consumes the result
# are both somebody else's problem.
[dependencies]

[lints]
workspace = true
